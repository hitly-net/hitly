# @hitly/core

Canonical Hitly types: `ApprovalEnvelope`, plugin interface, and edition entitlements.

This package is an SDK (`hitly.role: sdk`). Workspace consumers import TypeScript source; npm publish waits on a `dist/` build.
